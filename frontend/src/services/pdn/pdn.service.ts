export const PDNService = {
  async getByApplicationId(applicationId: number) {
    // const response = await instance<IReportBKI>({
    //   url: `/report-bki/${applicationId}`,
    //   method: "GET",
    // });
    const data = 100;
    return data;
    // return response.data;
  },
};
